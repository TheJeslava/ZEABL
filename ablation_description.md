# 消融描述

## 总表

| 版本名 | 总加权准确率 | 相对父版本的消融改动 |
|---|---:|---|
| u02 | **36.2597%** | `v02` + 基线 | 
| u03 | **38.3377%** | `u02` + 有题面框时去掉 stage2 中的 stage1 think 内容 |
| u09 | **39.3766%** | `u03` + 有题面框时使用更精简的 stage2 中段 prompt |
| u10 | **44.0519%** | `u03` + 有题面框时 stage2 仅输入 crop image |
| u05 | **40.4156%** | `u03` + 同时采用 u09 和 u10 的两项改动 |
| v02 | **33.5455%** | `u02` + 有题面框时，stage2 的 bbox 来源由直接使用 stage1 生成的 bbox 改为直接使用代码从题目提取的 bbox |
| v03 | **44.7013%** | `u03` + 有题面框时，stage2 的 bbox 来源由直接使用 stage1 生成的 bbox 改为直接使用代码从题目提取的 bbox |
| v09 | **45.6104%** | `u09` + 有题面框时，stage2 的 bbox 来源由直接使用 stage1 生成的 bbox 改为直接使用代码从题目提取的 bbox |
| v10 | **56.0779%** | `u10` + 有题面框时，stage2 的 bbox 来源由直接使用 stage1 生成的 bbox 改为直接使用代码从题目提取的 bbox |
| v05 | **59.9481%** | `u05` + 有题面框时，stage2 的 bbox 来源由直接使用 stage1 生成的 bbox 改为直接使用代码从题目提取的 bbox |

## u02 与官方实现的准确表述

准确表述如下：

> `u02` 的有题面框和无题面框路径并非完全相同：有题面框时，代码会先解析并将题目中的数值 bbox 换算到 stage1 全局图坐标；无题面框时则直接使用原问题。除此之外，两条路径均由 stage1 模型生成 `model_bbox_raw`，再使用同一套 bbox 映射、裁剪和 global image + crop image 的 stage2 流程，题面 bbox 不直接用于裁剪。
>
> 与官方 ZoomEarth 实现相比，`u02` 应描述为“保留官方主动裁剪两阶段核心逻辑的 XLRS 适配版本”，而不是逐项完全复现。主要差异包括：
>
> 1. `u02` 的 stage1 全局图最长边为 `1024`，官方论文和仓库为 `512`；
> 2. `u02` 将官方开放式回答 prompt 适配为 XLRS 选择题 prompt，加入选项和严格的字母输出协议；
> 3. `u02` 增加了对 XLRS 题目中数值 bbox 的代码解析和坐标重写，这不是官方 LRS-GRO 推理代码中的处理。
>
> 因此，`u02` 的核心逻辑与官方 ZoomEarth 一致，但 prompt、输入分辨率和题面 bbox 预处理均属于本地 XLRS 适配。
