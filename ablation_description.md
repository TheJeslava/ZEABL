# 消融描述

## 总表

| 版本名 | 总加权准确率 | 相对父版本的消融改动 |
|---|---:|---|
| p02 | **32.3117%** | 基线 |
| p03 | **41.5065%** | `p02` + 有题面框时去掉 stage2 中的 stage1 think 内容 |
| p09 | **44.0909%** | `p03` + 有题面框时使用更精简的 stage2 中段 prompt |
| p10 | **55.4026%** | `p03` + 有题面框时 stage2 仅输入 crop image |
| p05 | **54.7143%** | `p03` + 同时采用 p09 和 p10 的两项改动 |
| x02 | **36.0130%** | `p02` + 将无题面框题也固定为有题面框时的 stage2 构建协议（该父版本两条路径本已一致） |
| x03 | **41.3506%** | `p03` + 将去掉 stage1 think 的 stage2 构建协议扩展到全部题型 |
| x09 | **45.2078%** | `p09` + 将精简的 global image + crop image stage2 构建协议扩展到全部题型 |
| x10 | **54.5584%** | `p10` + 将仅输入 crop image 的 stage2 构建协议扩展到全部题型 |
| x05 | **57.1948%** | `p05` + 将精简的 crop-only stage2 构建协议扩展到全部题型 |
| v02 | **33.5455%** | `p02` + 有题面框时，将 stage1 题面 bbox 换算到全局图坐标，并将 stage2 的 bbox 来源由 stage1 生成的 bbox 改为代码从题目提取的 bbox |
| v03 | **44.7013%** | `p03` + 有题面框时，将 stage1 题面 bbox 换算到全局图坐标，并将 stage2 的 bbox 来源由 stage1 生成的 bbox 改为代码从题目提取的 bbox |
| v09 | **45.6104%** | `p09` + 有题面框时，将 stage1 题面 bbox 换算到全局图坐标，并将 stage2 的 bbox 来源由 stage1 生成的 bbox 改为代码从题目提取的 bbox |
| v10 | **56.0779%** | `p10` + 有题面框时，将 stage1 题面 bbox 换算到全局图坐标，并将 stage2 的 bbox 来源由 stage1 生成的 bbox 改为代码从题目提取的 bbox |
| v05 | **59.9481%** | `p05` + 有题面框时，将 stage1 题面 bbox 换算到全局图坐标，并将 stage2 的 bbox 来源由 stage1 生成的 bbox 改为代码从题目提取的 bbox |

总加权准确率按 XLRS-lite 全部 13 类的 3,080 条类别规模加权：p 系列结果覆盖 3 个题面框类别，其余 10 类继承对应 v 路径的结果；x 系列结果来自 seed `20260721` 的 13 类各 50 条、共 650 条实验。

## p02 与官方实现的准确表述

准确表述如下：

> `p02` 的有题面框和无题面框路径并非完全相同：有题面框时，stage1 保留题目中的原始图像分辨率和数值 bbox；无题面框时则直接使用不含 bbox 的原问题。两条路径均由 stage1 模型生成 `model_bbox_raw`，再进行裁剪并使用 global image + crop image 的 stage2 流程。对于有题面框的问题，模型 bbox 按题面声明的坐标空间映射到原图；题面 bbox 本身不直接用于裁剪。
>
> 与官方 ZoomEarth 实现相比，`p02` 应描述为“保留官方主动裁剪两阶段核心逻辑的 XLRS 适配版本”，而不是逐项完全复现。主要差异包括：
>
> 1. `p02` 的 stage1 全局图最长边为 `1024`，官方论文和仓库为 `512`；
> 2. `p02` 将官方开放式回答 prompt 适配为 XLRS 选择题 prompt，加入选项和严格的字母输出协议。
>
> 因此，`p02` 的核心逻辑与官方 ZoomEarth 一致，但 prompt 和输入分辨率属于本地 XLRS 适配。
